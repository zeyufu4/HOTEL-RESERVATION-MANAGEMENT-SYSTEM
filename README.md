This is our 5003 project.
How to run:
1.Use the git to download this project;
2.Run the hotel(1).sql;
3.Create a virtual environment,import the required dependencies;
4.Run the command in the terminal：python manage.py createsuperuser      python manage.py changepassword admin;
5.start the python project;
6.Use the superuser to login the system.
